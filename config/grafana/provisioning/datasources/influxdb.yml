apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influx:8086
    jsonData:
      version: Flux
      organization: default
      defaultBucket: default
      tlsSkipVerify: true
    secureJsonData:
      token: my-super-secret-admin-token
