syntax = "proto3";

package peer;

option go_package = "internal/peer";

message ModelUpdate {
	string source = 1;
	bytes weights = 2;
	int64 age = 3;
}

message PeerInfo {
	string id = 1;
	string fingerprint = 2;
}
